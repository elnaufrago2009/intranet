<%= form_for(@archivo, :html => {:class => "form-horizontal"}) do |f| %>
  <% if @archivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archivo.errors.count, "error") %> prohibited this archivo from being saved:</h2>

      <ul>
      <% @archivo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <div class="control-label">
        <%= f.label :tipodocumento_id %>
      </div>
      <div class="controls">
        <%= f.select(:tipodocumento_id, [
                ['memo'],
                ['oficio']
        ]) %>
      </div>
    </div>

  <div class="control-group">
    <div class="control-label">
      <%= f.label :nombre %>
    </div>
    <div class="controls">
      <%= f.text_field :nombre, :class => "span5" %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
      <%= f.submit :class => "btn" %>
    </div>
  </div>
<% end %>
